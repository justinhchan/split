node_modules
dist
out
# `build/` here is electron-builder's `buildResources` input — entitlements
# file + .icns/.ico/.png icons — not a build output. It must be tracked so
# codesigning has the entitlements plist available in CI.
.DS_Store
.eslintcache
*.log*
vitest.config.mts.timestamp-*.mjs
*.tsbuildinfo
coverage
.env
.env.local
.env.*.local
release/
